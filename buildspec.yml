version: 0.2

phases:
  install:
    commands:
      - npm ci
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 sync build/ s3://qweasdzxciopjklbnm/ --delete

artifacts:
  files:
    - '**/*'
